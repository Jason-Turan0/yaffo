fastapi>=0.111
uvicorn[standard]>=0.30
cryptography>=42
jinja2>=3.1
pytest>=8
pytest-asyncio>=0.24
aioquic>=1.3
# WS client for device<->hub signaling (uvicorn[standard] already ships the
# server side; this pins the client import explicitly).
websockets>=12

# Only needed for the Cloud Run rendezvous deployment (rendezvous_store.py's
# FirestoreRegistryStore) — local dev and tests never import it.
google-cloud-firestore>=2.16
