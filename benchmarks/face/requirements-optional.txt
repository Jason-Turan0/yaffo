# Additional backends. Install on top of requirements.txt; the runner skips any
# backend whose deps are missing, so add these incrementally.
#
# dlib is a source build (needs a compiler; pin setuptools<81 per the yaffo notes).
# It's the current production baseline (HOG detector + 128-d embedder).
dlib==20.0.0
face-recognition==1.3.0

# MediaPipe BlazeFace detector (very fast, detector-only here).
mediapipe

# facenet-pytorch: MTCNN detector + FaceNet (InceptionResnetV1) embedder. Pulls torch.
facenet-pytorch
torch

# DeepFace wraps many models (TensorFlow backend); heaviest / most conflict-prone.
# deepface
# tf-keras
