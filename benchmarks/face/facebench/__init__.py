"""facebench: a standalone harness to compare face detection + recognition
libraries (accuracy and wall time) on the Turan-Benchmark photo set. Lives in its
own venv so the heavy/conflicting ML deps never touch the main yaffo environment."""
