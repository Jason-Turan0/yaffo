__pycache__/

# Transient docs-pipeline output: staged captures, diff overlays, API run logs.
# A sibling of user_doc_automation/ so it is not inside the tree granted to the
# agent's filesystem tool.
.doc-staging/
/coverage*
