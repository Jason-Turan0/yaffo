feature: settings
description: User can configure media directories, the thumbnail directory, language and units, classification labels, and view system information

tags:
  - core

preconditions:
  - Application is running

context:
  - tag: settings-template
    path: templates/settings/index.html
    description: Settings page — media dirs, thumbnail dir, language, units, labels, LLM, system info
  - tag: settings-routes
    path: routes/settings.py
    description: Routes for media dirs, thumbnail stats/dir, locale, distance unit, labels, and LLM config
  - tag: settings-js
    path: static/settings/index.js
    description: Client wiring for adding/removing media dirs and streaming thumbnail stats
  - tag: responsive-nav-js
    path: static/nav.js
    description: Narrow-screen Menu and the peer page-panel contract (moves/restores live panel DOM)
  - tag: responsive-nav-panel-template
    path: templates/components/nav_panel.html
    description: Declares a page panel and its peer navbar button; the registration contract
  - tag: responsive-shell
    path: static/responsive.css
    description: Shared adaptive layout; owns the narrow-screen rules every page inherits

data:
  - Removing the last media directory should be allowed but leaves indexing with nothing to scan
  - Changing the language re-renders the UI text in the chosen locale

scenarios:
  - name: settings_add_media_directory
    goal: The user can add a media directory
    priority: high
    steps:
      - Navigate to the settings page
      - Enter a valid directory path into the new media directory field
      - Click the "Add Directory" button
    verify:
      - The new directory appears in the Media Directories list
      - A success message is displayed
      - Entering an empty path shows a validation error instead of adding
    cleanup:
      - Remove the media directory that was added

  - name: settings_remove_media_directory
    goal: The user can remove a media directory after confirming
    priority: high
    steps:
      - Navigate to the settings page
      - Click Remove on one of the listed media directories
      - Confirm the removal in the confirmation dialog
    verify:
      - A confirmation dialog is shown before removing
      - After confirming, the directory disappears from the Media Directories list
      - A success message is displayed
      # The "No media directories configured" empty state requires removing the
      # seeded library directory, which the parallel suites depend on — not asserted.
      # Tests add their own scratch directory and remove that one.

  - name: settings_change_language
    goal: Changing the application language re-renders the UI in that locale
    priority: high
    steps:
      - Navigate to the settings page
      - Select a different language from the "Application language" selector
    verify:
      - The page reloads/updates and visible UI text is shown in the selected language
      - The selected language remains chosen after navigating to another page
    cleanup:
      - Restore the language back to the original value

  - name: settings_change_distance_unit
    goal: The user can switch the preferred distance unit
    priority: medium
    steps:
      - Navigate to the settings page
      - Change the "Preferred distance unit" selector
    verify:
      - The distance unit preference is saved and survives a reload
      # Distance rendering elsewhere (locations, automation distance fields) reads
      # the preference live; asserting it would couple this suite to those pages,
      # so persistence is the verified contract.
    cleanup:
      - Restore the original distance unit

  - name: settings_change_thumbnail_directory
    goal: The user can move the thumbnail directory to a new location
    priority: medium
    steps:
      - Navigate to the settings page
      - Enter a new thumbnail directory path
      - Click "Change Directory" / "Set Directory"
      - Confirm the move when prompted with the file count and size
    verify:
      - The thumbnail stats (file count and total size) stream in and populate
      - The confirmation dialog shows how many files and how much data will move
      - After confirming, the Current Thumbnail Directory updates to the new path
      - A success message is displayed
    cleanup:
      - Restore the thumbnail directory to the original path

  - name: settings_manage_classification_labels
    goal: The user can edit the image-classification label vocabulary
    priority: medium
    steps:
      - Navigate to the settings page
      - Add a new classification label to the vocabulary
      - Save the labels
    verify:
      - The new label appears in the label vocabulary
      - A success message is displayed
      - A removed label no longer appears in the vocabulary
    cleanup:
      - Remove the label that was added

  - name: settings_system_information_displayed
    goal: The System Information section shows environment and model paths
    priority: low
    steps:
      - Navigate to the settings page
      - Scroll to the System Information section
    verify:
      - Build Version and Build Timestamp are displayed
      - Database Path and Task Queue Database Path are shown
      - ExifTool, FFmpeg, Face Recognition, and Image Classification model paths are listed (or "Not found")
  # ---------------------------------------------------------------------------
  # Responsive coverage (P7 — settings). The shared shell contract itself is verified
  # in specs/photo_gallery.yaml; what follows is this family's own narrow-screen
  # behaviour. Shared helpers: generated_tests/_support/responsive.ts.
  #
  # Settings registers no page panel of its own (it has no sidebar), so the
  # peer-panel part of the page-family minimum is covered by specs/themes.yaml
  # instead — themes is the other half of this page family.
  # ---------------------------------------------------------------------------
  - name: settings_route_fits_every_contract_viewport
    goal: The settings page renders without page-level horizontal overflow at every supported width
    priority: high
    steps:
      - Load the settings page at 320, 390, 768, 1024 and 1440 CSS pixels
    verify:
      - The document never scrolls horizontally at any of the contract widths
  - name: settings_long_paths_wrap_instead_of_widening_the_page
    goal: Filesystem paths and API keys stay inside the viewport
    priority: high
    steps:
      - Load the settings page at 320x568
      - Replace a media directory row and a System Information value with a long break-free path
    verify:
      - System and current path values wrap anywhere rather than overflowing
      - The page does not scroll horizontally
      - Destructive actions stay visually distinct from the rest once the rows stack
      - The Remove control does not become the widest element in a stacked directory row
  - name: settings_label_help_icons_stay_visible_below_the_breakpoint
    goal: The label prompt and English-only markers remain real, visible controls on a phone
    priority: high
    steps:
      - Load the settings page at 320x568, 390x844 and 768x1024
      - Measure the Photo labels help tips
    verify:
      - Every help tip has a non-zero box at every width
      # Regression. static/responsive.css hides `[data-tooltip]::before` below
      # 640px to drop the JS tooltip's arrow, but on a help tip that ::before is
      # the icon glyph itself, so every prompt and warning marker collapsed to a
      # 0x0 invisible control on phones.
  - name: settings_label_prompt_tooltips_do_not_widen_the_page
    goal: A label's prompt bubble stays inside the viewport instead of pushing the page sideways
    priority: high
    steps:
      - Load the settings page at 768x1024, 1024x768 and 1440x900
      - Hover the last chip's prompt marker
    verify:
      - The page does not scroll horizontally before or while the bubble is shown
      - The bubble is no wider than the viewport
      # Regression. The shared pure-CSS tooltip is a 260px box centred on its
      # anchor; a chip's marker is 15px wide and the chips wrap right up to the
      # section's inline end, so the bubble hung 48px past the document edge and
      # the settings page scrolled sideways with nothing hovered.
  - name: settings_label_prompts_open_on_a_coarse_pointer
    goal: Tapping a label's prompt marker reveals it, and the chip controls are real touch targets
    priority: high
    steps:
      - Load the settings page in a coarse-pointer context at 390x844
      - Tap a label chip's prompt marker
    verify:
      - The prompt marker and the remove control are at least 44 CSS pixels in both directions
      - Tapping the marker reveals the prompt, which hover and :focus-visible alone never do on touch
      - The page does not scroll horizontally
  - name: settings_api_key_controls_stack_without_crowding_the_destructive_action
    goal: The API-key row is a real action row that stacks on a phone
    priority: medium
    steps:
      - Load the settings page at 1440x900 and at 320x568
    verify:
      - The API-key actions are a flex row with a gap, so Save and Clear cannot butt against each other
      - At 320 the actions stack and each control spans the column
      - The API-key status line wraps an environment variable name rather than widening the section
  - name: settings_entered_values_survive_a_resize_through_the_breakpoint
    goal: Unsaved input is not lost when the viewport crosses the responsive breakpoint
    priority: high
    steps:
      - Enter a media directory path, a label filter and an API key at 390x844
      - Resize to 1440x900, 320x568, 844x390 and back to 1440x900
    verify:
      - All three entered values are still there after every resize
      - No page-level horizontal overflow appears at any of those sizes
  - name: settings_folder_picker_fits_a_narrow_viewport
    goal: The shared folder/file picker is usable on a narrow screen
    priority: high
    steps:
      - Load the settings page at 390x844 and open a folder picker
      - Press Escape
    verify:
      - The picker fits inside the viewport — a full-width sheet on a phone, an edge gutter on a tablet
      - The entry list scrolls inside itself using dynamic viewport units and contains its own overscroll
      - Long paths in the picker bar wrap or ellipsise rather than widening the dialog
      - Escape closes the picker
  - name: settings_translated_labels_do_not_widen_the_page
    goal: German and Arabic settings copy fits every contract width
    priority: medium
    steps:
      - Switch the application language to German, then to Arabic
      - Load the settings page at each contract width
    verify:
      - html lang and dir follow the chosen locale
      - No page-level horizontal overflow in either locale at any contract width
    cleanup:
      - Restore the original application language
  - name: settings_standalone_screens_fit_and_keep_their_action_reachable
    goal: The error and request-not-verified screens obey the same width and target guarantees
    priority: high
    steps:
      - Load a missing route at every contract width
      - Submit a settings form with its CSRF token blanked, at 320 and at 1440
    verify:
      - Neither screen scrolls horizontally at any contract width
      - The error screen's "Back to your library" action is at least 44 CSS pixels tall and inside the viewport
      # Regression. The request-not-verified shell loads its own stylesheets and
      # never linked button.css, so its only action rendered as a bare 18px link
      # with no fill, no padding and no touch target.
      - The request-not-verified screen's action is a filled, padded button
